<p id="notice"><%= notice %></p>

<h1>Listas</h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Notas</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_lists.each do |user_list| %>
      <tr>
        <td><%= user_list.name %></td>
        <td><%= user_list.notes %></td>
        <td><%= link_to 'Ver Lista', user_list %></td>
        <td><%= link_to 'Editar Lista', edit_user_list_path(user_list) %></td>
        <td><%= link_to 'Escolher Produtos', choose_products_user_lists_path(user_list) %></td>
        <td><%= link_to 'Excluir Lista', user_list, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Lista', new_user_list_path %>
